Module Start
   i = 0
   WhileLoop:
   if variable i is greater than or equal to 10 then goto WhileLoopDone
      ForLoop:
         if variable is out of range(3) then goto ForLoopDone
         call function print
         input arg:j
         goto ForLoop
      ForLoopDone
      i + 1
      i = (i + 1)
      goto WhileLoop
   WhileLoopDone
Module Done